1. Fix duplicate "Reset Password" quick actions in `xpress-app/src/Pages/Management/Customers/CustomerDetailModal.jsx` by changing one to "Block User" or a more appropriate action if it's supposed to be something else. (Already partially fixed by script, but should be confirmed).
2. Fix ESLint warnings/errors and clean up code. (Partially done, need to clear the rest).
3. Check `TODO` in `xpress-app/src/orderSlice.js` about "Implement email notifications for admins here".
4. Pre-commit check and then submit.
