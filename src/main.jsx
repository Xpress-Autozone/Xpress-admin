import { BrowserRouter } from 'react-router-dom';

// Set BrowserRouter basename
const App = () => (
    <BrowserRouter basename="/xpress-admin">
        {/* other components */}
    </BrowserRouter>
);

export default App;